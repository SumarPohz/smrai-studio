<!-- FOOTER VERSION: 2025-01-XX NO WHATSAPP -->
</main>

<footer class="site-footer">
  <div class="footer-container">
    <div class="footer-columns">
      <div class="footer-col">
        <h4>INFORMATION</h4>
        <a href="/about"  class="footer-card">About Us</a>
        <a href="/news"   class="footer-card">News</a>
        <a href="/terms-privacy"  class="footer-card">Terms &amp; Privacy Policy</a>
      </div>

      <div class="footer-col">
        <h4>ACCOUNT</h4>
        <a href="/login"    class="footer-card">Login</a>
        <a href="/register" class="footer-card">Register</a>
      </div>

      <div class="footer-col">
        <h4>CONNECT</h4>
        <a href="/contact" class="footer-card">Contact</a>
        <a href="/support" class="footer-card">Support</a>
      </div>

      <div class="footer-col">
        <h4>HELP</h4>
        <a href="/help" class="footer-card">Help Center</a>
        <a href="/faq"  class="footer-card">FAQ</a>
        <a href="/support" class="footer-card">Contact Support</a>
      </div>

      <div class="footer-col footer-social">
        <h4>CONNECT WITH US</h4>
        <div class="social-icons">
          <a href="https://www.facebook.com/" aria-label="Facebook"><i class="fab fa-facebook-f"></i></a>
          <a href="https://x.com/" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
          <a href="https://www.instagram.com/" aria-label="Instagram"><i class="fab fa-instagram"></i></a>
          <a href="https://www.linkedin.com/in/sumarpohz" aria-label="LinkedIn"><i class="fab fa-linkedin-in"></i></a>
        </div>
      </div>
    </div>

    <div class="footer-bottom">
      <p>© <%= new Date().getFullYear() %> SmrAI-Studio. All rights reserved.</p>
    </div>
  </div>
</footer>

<!-- Font Awesome (CDN version, no API key needed) -->
<link
  rel="stylesheet"
  href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
/>

<style>
  /* ====== FOOTER ====== */
  .site-footer {
    background: #e5e7eb;
    border-top: 1px solid #d1d5db;
    padding: 1rem 0 0.45rem;   /* ↓ was 2.5rem 0 1.25rem */
    margin-top: 1.5rem;            /* ↓ was 3rem */
    font-family: "Segoe UI", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
  }

  /* Full-width grey bar, content has side padding */
  .footer-container {
    width: 100%;
    padding: 0 2rem;
    box-sizing: border-box;
  }

  .footer-columns {
    width: 100%;               /* stretch with screen */
    margin: 0 auto;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 1rem;                 /* ↓ was 1.5rem */
    padding-bottom: 0.75rem;   /* ↓ was 1.25rem */
    border-bottom: 1px solid #d1d5db;
  }

  .footer-col {
    flex: 1 1 160px;
    min-width: 160px;
  }

  .footer-col h4 {
    font-size: 0.9rem;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    margin-bottom: 0.6rem;     /* slightly tighter */
    color: #111827;
  }

  .footer-card {
    display: block;
    margin-bottom: 0.35rem;    /* ↓ was 0.5rem */
    padding: 0.45rem 0.8rem;   /* ↓ was 0.55rem 0.9rem */
    border-radius: 12px;
    background: #ffffff;
    color: #111827;
    text-decoration: none;
    font-size: 0.9rem;
    box-shadow: 0 1px 2px rgba(15, 23, 42, 0.06);
    border: 1px solid #e5e7eb;
    transition: transform 0.12s ease, box-shadow 0.12s ease, border-color 0.12s ease;
  }

  .footer-card:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 8px rgba(15, 23, 42, 0.12);
    border-color: #facc15;
  }

  /* ====== SOCIAL ICONS ====== */
  .footer-social {
    text-align: left;
  }

  .social-icons {
    margin-top: 0.6rem;        /* ↓ was 0.8rem */
    display: flex;
    gap: 0.6rem;
  }

  .social-icons a {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: #d1d5db;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #111827;
    font-size: 0.9rem;
    transition: all 0.25s ease;
  }

  .social-icons a:hover {
    background: #facc15; /* gold hover for AI theme */
    color: #111827;
    transform: translateY(-2px);
  }

  .footer-bottom {
    width: 100%;               /* stretch with screen */
    margin: 0.5rem auto 0;     /* ↓ was 1rem */
    text-align: center;
    font-size: 0.85rem;
    color: #6b7280;
  }

  /* ====== RESPONSIVE ====== */
  @media (max-width: 768px) {
    .footer-container {
      padding: 0 1rem; /* slight padding on smaller screens */
    }

    /* keep row layout even on mobile */
    .footer-columns {
      flex-direction: row;
      flex-wrap: wrap;
      justify-content: space-between;
      gap: 0.8rem;            /* a bit tighter on mobile */
    }

    .footer-col {
      flex: 1 1 150px;
      min-width: 150px;
      width: auto;
    }

    /* make cards look like plain text links on mobile */
    .footer-card {
      background: transparent;
      border: none;
      box-shadow: none;
      border-radius: 0;
      padding: 0.35rem 0.6rem; /* ↓ slightly smaller */
      margin-bottom: 0.15rem;
      font-size: 0.85rem;
    }

    .footer-card:hover {
      transform: none;
      box-shadow: none;
      border-color: transparent;
      text-decoration: underline;
    }

    .footer-col h4 {
      margin-bottom: 0.3rem;
    }

    .social-icons {
      margin-top: 0.35rem;
    }

    .social-icons a {
      width: 30px;
      height: 30px;
    }

    .footer-bottom {
      margin-top: 0.4rem;
      padding-top: 0.4rem;
      border-top: 1px solid #d1d5db;
      font-size: 0.8rem;
    }
  }
</style>
<% if (currentUser) { %>
<!-- Floating WhatsApp Support Button — logged-in users only -->
<a
  href="https://wa.me/918787509536?text=Hi%20Sumar%2C%20I%20need%20support%20regarding%20SmrAI-Studio."
  class="whatsapp-float"
  target="_blank"
  rel="noopener noreferrer"
  aria-label="WhatsApp Support"
>
  <i class="fab fa-whatsapp"></i>
</a>
<% } else { %>
<!-- ── Guest chat widget ──────────────────────────────────────────────────── -->
<button id="gcBtn" class="gc-float" aria-label="Chat with us" type="button">
  <i class="fas fa-comment-dots"></i>
</button>

<div id="gcBox" class="gc-box" hidden>
  <div class="gc-header">
    <div class="gc-header-info">
      <div class="gc-avatar"><i class="fas fa-headset"></i></div>
      <div>
        <div class="gc-title">SmrAI Support</div>
        <div class="gc-sub">We usually reply within a few hours</div>
      </div>
    </div>
    <button class="gc-close" id="gcClose" type="button" aria-label="Close">&times;</button>
  </div>

  <!-- Pre-chat form -->
  <div id="gcPreChat" class="gc-pre-chat">
    <p class="gc-pre-desc">Enter your details to start chatting with us</p>
    <form id="gcPreForm">
      <input type="text"  id="gcName"  class="gc-field" placeholder="Your name"  required maxlength="100" autocomplete="name">
      <input type="email" id="gcEmail" class="gc-field" placeholder="Your email" required maxlength="200" autocomplete="email">
      <button type="submit" class="gc-start-btn" id="gcStartBtn">Start Chat <i class="fas fa-arrow-right"></i></button>
    </form>
  </div>

  <!-- Chat area (hidden until pre-chat done) -->
  <div id="gcChat" class="gc-chat" hidden>
    <div class="gc-messages" id="gcMessages">
      <div class="gc-empty">No messages yet — say hello! 👋</div>
    </div>
    <form class="gc-input-bar" id="gcForm" autocomplete="off">
      <textarea class="gc-input" id="gcInput" rows="1" placeholder="Type a message…" maxlength="2000"></textarea>
      <button class="gc-send" type="submit" aria-label="Send"><i class="fas fa-paper-plane"></i></button>
    </form>
  </div>
</div>

<script>
(function () {
  const gcBtn   = document.getElementById("gcBtn");
  const gcBox   = document.getElementById("gcBox");
  const gcClose = document.getElementById("gcClose");

  // ── Toggle open/close ──────────────────────────────────────────────────────
  gcBtn.addEventListener("click", () => { gcBox.hidden = !gcBox.hidden; });
  gcClose.addEventListener("click", () => { gcBox.hidden = true; });

  // ── Helpers ────────────────────────────────────────────────────────────────
  function gcEsc(str) {
    return String(str).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;");
  }

  let gcLastId = 0;

  function gcAppend(m) {
    const el     = document.getElementById("gcMessages");
    const empty  = el.querySelector(".gc-empty");
    if (empty) empty.remove();
    const isMe   = m.sender_role === "user";
    const time   = new Date(m.created_at).toLocaleTimeString("en-IN", { hour:"2-digit", minute:"2-digit" });
    const name   = isMe ? "" : (m.sender_name || "Support");
    const row    = document.createElement("div");
    row.className = `gc-bubble-row ${isMe ? "mine" : "theirs"}`;
    row.innerHTML = `
      ${!isMe ? `<div class="gc-sender-name">${gcEsc(name)}</div>` : ""}
      <div class="gc-bubble ${isMe ? "mine" : "theirs"}">${gcEsc(m.message)}</div>
      <div class="gc-bubble-meta">${time}</div>`;
    el.appendChild(row);
    el.scrollTop = el.scrollHeight;
    if (m.id > gcLastId) gcLastId = m.id;
  }

  // ── Polling ────────────────────────────────────────────────────────────────
  let gcPollTimer = null;

  function gcStartPolling(requestId, token) {
    clearInterval(gcPollTimer);
    gcPollTimer = setInterval(async () => {
      try {
        const r = await fetch(`/api/guest/chat/${requestId}/messages?token=${encodeURIComponent(token)}`);
        const d = await r.json();
        if (!d.success) return;
        d.messages.forEach(m => { if (m.id > gcLastId) gcAppend(m); });
      } catch (_) {}
    }, 6000);
  }

  // ── Load history + start polling ───────────────────────────────────────────
  function gcStartChat(requestId, token) {
    document.getElementById("gcPreChat").hidden = true;
    document.getElementById("gcChat").hidden    = false;
    fetch(`/api/guest/chat/${requestId}/messages?token=${encodeURIComponent(token)}`)
      .then(r => r.json())
      .then(d => {
        if (!d.success) return;
        d.messages.forEach(gcAppend);
      })
      .catch(() => {});
    gcStartPolling(requestId, token);
  }

  // ── Restore session from localStorage ─────────────────────────────────────
  try {
    const stored = JSON.parse(localStorage.getItem("gcSession") || "null");
    if (stored && stored.requestId && stored.token) {
      gcStartChat(stored.requestId, stored.token);
    }
  } catch (_) {}

  // ── Pre-chat form submit ───────────────────────────────────────────────────
  document.getElementById("gcPreForm").addEventListener("submit", async function (e) {
    e.preventDefault();
    const btn   = document.getElementById("gcStartBtn");
    const name  = document.getElementById("gcName").value.trim();
    const email = document.getElementById("gcEmail").value.trim();
    btn.disabled = true;
    btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Starting…';
    try {
      const r = await fetch("/api/guest/start-chat", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ name, email }),
      });
      const d = await r.json();
      if (!d.success) { alert("Could not start chat. Please try again."); return; }
      localStorage.setItem("gcSession", JSON.stringify({ requestId: d.requestId, token: d.token, name }));
      gcStartChat(d.requestId, d.token);
    } catch (_) {
      alert("Network error. Please try again.");
    } finally {
      btn.disabled = false;
      btn.innerHTML = 'Start Chat <i class="fas fa-arrow-right"></i>';
    }
  });

  // ── Send message ───────────────────────────────────────────────────────────
  document.getElementById("gcForm").addEventListener("submit", async function (e) {
    e.preventDefault();
    const input = document.getElementById("gcInput");
    const msg   = input.value.trim();
    if (!msg) return;
    try {
      const stored = JSON.parse(localStorage.getItem("gcSession") || "null");
      if (!stored) return;
      // Optimistic append
      gcAppend({ id: Date.now(), sender_role:"user", message: msg, created_at: new Date().toISOString() });
      input.value = ""; input.style.height = "";
      await fetch("/api/guest/chat/message", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ requestId: stored.requestId, token: stored.token, message: msg }),
      });
    } catch (_) {}
  });

  // ── Auto-grow textarea + Enter to send ────────────────────────────────────
  const gcInput = document.getElementById("gcInput");
  gcInput.addEventListener("input", function () {
    this.style.height = "auto";
    this.style.height = Math.min(this.scrollHeight, 100) + "px";
  });
  gcInput.addEventListener("keydown", function (e) {
    if (e.key === "Enter" && !e.shiftKey) {
      e.preventDefault();
      document.getElementById("gcForm").requestSubmit();
    }
  });
})();
</script>
<% } %>

</body>
</html>
